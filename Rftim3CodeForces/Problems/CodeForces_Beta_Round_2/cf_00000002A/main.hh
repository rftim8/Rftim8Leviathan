#pragma once
#include "rft_codeforces.hh"

static int _n, _m;
static std::vector<std::vector<int>> _v;
static std::vector<int> _v0;

static void dataCollector()
{
    in >> _n;
    in >> _m;

    for (int i = 0; i < _n * _m; i += _m)
    {
        
    }
};

int cf_00000002A_0(std::vector<int> nums);
void cf_00000002A_0_test(int case_nr, int actual, int expected);
